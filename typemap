const char *			T_PV
bz_object *				T_BZ_OBJECT

#############################################################################
INPUT

T_UV
	$var = (unsigned long)SvUV($arg)
T_BZ_OBJECT
	$var = stream_self($arg)

#############################################################################
OUTPUT

T_UV
	sv_setuv($arg, (IV)$var);
